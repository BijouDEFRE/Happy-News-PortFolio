// == Import npm
import React from 'react';

// == Import CSS
import './spinner.scss';

// Just a simple Spinner shown by our teacher, a way to wink at him!
const Spinner = () => (
  <div className="spinner" />
);

export default Spinner;
